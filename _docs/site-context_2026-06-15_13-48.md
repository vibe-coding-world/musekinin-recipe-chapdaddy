---
id: TH_2026-06-15_13-48_
type: think-act-item-timeline
title: create-context
datetime: 2026-06-15 13:48
---


## プロジェクト

料理サイト「無責任レシピ Chapdaddy」。

作業場所:
/Users/Shared/_htdocs/2026/02/P_2026-02-09_cityblend.jpは作成する。Webサイトを。料理Chapdaddyの/htdocs

## 出力ルール

Simple / Concise。  
図解はmermaid優先。  
複数要素の関係は図で示す。

## Git

ローカルGitを使用。  
GitHubは使わない方針。

`.gitignore` 作成済み。  
`.DS_Store` は除外済み。

## ローカル確認

サーバーは以下で起動していた。

```bash
python3 -m http.server 8000
```

確認URL:

```text
http://127.0.0.1:8000/index.html
```

サーバーが落ちた場合は再起動する。

## Browserプラグイン

Codex Browser / in-app Browser は設定を試したが、この環境では使えなかった。

エラー:

```text
Browser is not available: iab
```

今後は手動Chrome確認 + HTTP/DOM確認で進める。

## 注意点

- ユーザーが途中で `css/shop-carousel.css`、`js/shop-banner.js`、`css/side-banners.css` を修正している。
- 既存変更を勝手に戻さない。
- 実装前に必ず対象ファイルを読み直す。
- CSS設計は `_docs/設計_共通.md` に従う。
- 専用CSSは `css/components_v2.css` に混ぜず、機能別CSSにする方針。